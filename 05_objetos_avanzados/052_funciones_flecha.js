// Las Arrow function nos permiten simplificar la notación de las funciones. No pueden devolver objetos.

function sumar (a, b) {
    return a + b;
}

const sumarArrow = (a, b) => a + b;

const restar = (a, b) => a - b;